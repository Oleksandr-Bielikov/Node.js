nav.navbar
  style.
    .navbar {
      background-color: #2c3e50;
      padding: 0.8rem 1.5rem;
      border-radius: 10px;
      display: flex;
      align-items: center;
      gap: 1.5rem;
      justify-content: center;
      box-shadow: 0 4px 6px rgba(0,0,0,0.1);
      margin-bottom: 30px;
    }

    .nav-item {
      display: flex;
      align-items: center;
    }

    .nav-list {
      list-style: none;
      display: flex;
      gap: 1rem;
      margin: 0;
      padding: 0;
    }

    .nav-link {
      color: #ecf0f1;
      text-decoration: none;
      font-weight: 500;
      padding: 0 12px;
      border-radius: 6px;
      transition: background 0.3s, color 0.3s;
      background: #34495e;
      display: flex;
      align-items: center;
      height: 42px;
      line-height: 1;
    }

    .nav-link:hover {
      background-color: #3d5a73;
      color: #f1c40f;
    }

    .filter-form {
      display: flex;
      align-items: center;
      gap: 0.6rem;
    }

    .filter-form label {
      color: #ecf0f1;
      display: flex;
      align-items: center;
      height: 42px;
      line-height: 1;
      padding: 0 5px;
    }

    .filter-form select,
    .filter-form button {
      height: 42px;
      line-height: 1;
      display: flex;
      align-items: center;
      padding: 0 12px;
      border-radius: 6px;
      border: none;
      cursor: pointer;
      font-size: 1rem;
      background: #34495e;
      color: #ecf0f1;
      transition: background 0.3s, color 0.3s;
      outline: none;
    }

    .filter-form select:hover,
    .filter-form button:hover {
      background: #3d5a73;
      color: #f1c40f;
    }

    #notesForm {
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    #notesForm label {
      display: flex;
      align-items: center;
      gap: 0.3rem;
      cursor: pointer;
      font-size: 1rem;
      color: #ecf0f1;
    }

    #notesForm input[type="checkbox"] {
      width: 18px;
      height: 18px;
      cursor: pointer;
      accent-color: #3498db;
    }


  ul.nav-list
    li.nav-item
      a.nav-link(href="/main") Скинути фільти
    li.nav-item
      a.nav-link(href="/main?sort=min") Сортування віку за зростанням
    li.nav-item
      a.nav-link(href="/main?sort=max") Сортування віку за спаданням

  form.filter-form(action="/main" method="GET")
    label(for="role") Оберіть категорію:
    select(name="role" id="role")
      option(value="all" selected=role=='all') Всі
      option(value="user" selected=role=='user') Юзер
      option(value="admin" selected=role=='admin') Адміністратор
      option(value="moderator" selected=role=='moderator') Модератор
      option(value="guest" selected=role=='guest') Гість
    button(type="submit") Фільтрувати

  form#notesForm(action="/main" method="GET")
    label(for="notes") Тільки з нотатками
      input(
        type="checkbox"
        name="notes"
        id="notes"
        checked=notesChecked
      )

script.
  const notesCheckbox = document.getElementById('notes');
  const notesForm = document.getElementById('notesForm');

  notesCheckbox.addEventListener('change', () => {
    notesForm.submit();
  });


div.tags-container
  style.
    .tags-container {
      display: flex;
      flex-wrap: wrap;
      gap: 0.5rem;
      margin-top: 15px;
      margin-bottom: 15px;
      justify-content: center;
    }

    .tags-container a {
      background-color: #3498db;
      color: #fff;
      padding: 5px 10px;
      border-radius: 6px;
      text-decoration: none;
      font-size: 0.9rem;
      transition: background 0.3s, color 0.3s;
    }

    .tags-container a:hover {
      background-color: #2980b9;
      color: #f1c40f;
    }

  each item in uniqueTags
    a(href=`/main?tag=${item}`) #{item}


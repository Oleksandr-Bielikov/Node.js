extends layout

block content 
    ul 
        if files && files.length > 0
            each item in files
                li 
                    // Тут ми використовуємо item.originalname для тексту
                    strong= item.originalname
                    |  (#{ (item.size / 1024).toFixed(2) } KB) - 
                    // А тут item.filename для посилання
                    a(href=`/download/${item.filename}`) Download ZIP
        else
            li No files uploaded yet.